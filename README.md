# Project X

A Python project template with src layout, pytest, and editable install.

## Setup

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -e .
pip install pytest
```

## Run

```bash
python src/project_x/main.py
```

## Test

```bash
pytest
```